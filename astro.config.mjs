import { defineConfig } from "astro/config";

export default defineConfig({
  output: "static", // Change to 'server' if using SSR
  site: "https://your-site.com", // Change to your actual domain
});
